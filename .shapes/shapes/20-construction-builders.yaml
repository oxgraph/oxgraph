id: 20
name: construction builders
description: |
  Append/update-only construction systems for ordinary directed graphs and directed
  hypergraphs. Builders assign dense append-only canonical IDs, accept isolated
  elements, parallel graph edges, and typed weights/properties before freeze, then
  export owned immutable views with local/canonical identity maps. They are builders,
  not general mutation overlays.
profile: 1
status: proposed
intent:
  kind: module
  summary: Append/update-only graph and hypergraph builders that freeze to immutable views
  source: ai
  goals: |
    - Add oxgraph-graph-build with isolated-node support, parallel edges, generic
      element/relation weights, named Arrow property layer attachment, canonical IDs,
      and owned freeze to a directed view.
    - Add oxgraph-hyper-build with directed source/target participants, incidence IDs,
      generic element/relation/incidence weights, named Arrow property layer attachment,
      canonical IDs, and owned freeze to a directed view.
    - Borrowed caches/views are invalidated by builder edits; owned frozen views remain
      independent after subsequent edits.
    - Deletion, tombstones, ID reuse, compaction, and overlay mutation remain out of
      scope.
  non_goals: |
    No full mutable graph API, no deletion, no stale-handle mutation policy beyond
    generation checks for borrowed caches, no cross-snapshot lineage, and no third-party
    exporters.
  rationale: |
    Python and downstream integrations need an ergonomic way to construct topology while
    preserving OxGraph's read-side view model. Append/update builders provide that
    producer-side path without prematurely committing to general mutation semantics.
constraints:
- 26
- 29
realization:
- bindings:
  - scheme: path
    value: crates/oxgraph-graph-build
    metadata:
      summary: Append/update-only ordinary directed graph builder and owned frozen view.
  - scheme: path
    value: crates/oxgraph-hyper-build
    metadata:
      summary: Append/update-only directed hypergraph builder and owned frozen view.
  role: primary
amendment_log:
- 9
- 10
parents:
- id: 16
  role: component
